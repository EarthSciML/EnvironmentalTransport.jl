name: "Tests"

on:
  pull_request:
    branches:
      - main
      - 'release-'
    paths-ignore:
      - 'docs/**'
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'

concurrency:
  # Skip intermediate builds: always, but for the master branch.
  # Cancel intermediate builds: always, but for the master branch.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  tests:
    name: "Tests - Julia ${{ matrix.version }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version:
          - "1"
          - "lts"
          #- "pre"
    steps:
      - uses: actions/checkout@v4

      - name: "Setup Julia ${{ matrix.version }}"
        uses: julia-actions/setup-julia@v2
        with:
          version: "${{ matrix.version }}"

      - uses: julia-actions/cache@v2
        with:
          token: "${{ secrets.GITHUB_TOKEN }}"

      - uses: julia-actions/julia-buildpkg@v1

      - name: "Install MethodOfLines fork with unit fixes"
        run: |
          git clone --branch fix-units-discretization https://github.com/ctessum-claude/MethodOfLines.jl.git /tmp/MethodOfLines.jl
          julia --project -e 'using Pkg; Pkg.develop(path="/tmp/MethodOfLines.jl")'

      - name: "Run tests"
        uses: julia-actions/julia-runtest@v1
        with:
          depwarn: "yes"
        env:
          JULIA_NO_VERIFY_HOSTS: 'gaftp.epa.gov'

      - uses: julia-actions/julia-processcoverage@v1
        with:
          directories: "src,ext"

      - name: "Report Coverage with Codecov"
        uses: codecov/codecov-action@v5
        with:
          files: lcov.info
          token: "${{ secrets.CODECOV_TOKEN }}"
          fail_ci_if_error: true
